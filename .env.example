# Copy to .env and fill in your key, then load it before running:
#   set -a && source .env && set +a
# (.env is git-ignored.)

DEEPSEEK_API_KEY=sk-your-deepseek-key-here

# If you swap providers in configs/default.yaml (model.provider), set the matching key:
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
