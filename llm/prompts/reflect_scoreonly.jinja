[SYSTEM]
You are refining your SKILL LIBRARY. You are shown ONLY the SCALAR performance (mean gap) of recent strategies/skills -- NOT the solutions themselves. Propose typed skill operations to improve.

SKILL CODE REQUIREMENTS (skills that do not match are silently unusable):
- The function MUST be named exactly `f` and match its kind's signature EXACTLY:
    construct:    def f(n, x, C, prims) -> list[int]      # a tour: a permutation of range(n)
    local_search: def f(tour, C, prims) -> list[int]      # an improved tour (same node set)
    repair:       def f(partial, n, C, prims) -> list[int]
    destroy:      def f(tour, C, prims, k) -> tuple        # (partial_tour, removed_nodes)
    diagnose:     def f(tour, x, C, prims) -> dict
    strategy:     def f(n, x, C, skills, prims) -> list[int]   # may call skills["<id>"](...)
- Read a cost as C[i][j] or C[i, j] (integer). Helpers: prims.tour_length(tour, C), prims.is_valid_tour(tour, n), prims.dist_matrix(coords). numpy is available as np.
- Self-contained pure function (numpy available as np). Must be GENERAL across instances of this family: infer any structure from C at run time (e.g. cluster nodes by cost, derive thresholds from C's own distribution) -- do NOT hard-code instance-specific magic numbers.
- CORRECTNESS: a construct/strategy MUST return a permutation of ALL of range(n), each node exactly once (a common bug is dropping the start node). Before returning, verify with prims.is_valid_tour(tour, n) and append any missing nodes. A local_search/repair MUST return the SAME node set it received.
- If your skill claims to exploit structure (e.g. clusters), the code must ACTUALLY USE that structure in its decisions -- not merely compute it and then ignore it.

[OUTPUT] Strict JSON, same schema as the inducer: {"analysis": str, "ops": [{"op":"add|modify|merge|prune","kind":"construct|local_search|repair|destroy|diagnose|strategy","target_id": str|null,"name":str,"description_nl":str,"contract":{"inputs":[str],"requires":[str],"preserves":[str],"produces":[str]},"code":"<python matching the kind signature>","claimed_improvement":str}]}

[USER]
Recent skill/strategy scores (id : kind : mean_gap : times_used):
{{scores_block}}
Current library (id : kind : description : reuse_rate):
{{library_summary}}
How your PREVIOUS proposals scored (learn from this -- do NOT repeat what was rejected):
{{curation_feedback}}
Propose 1-3 ops. Return ONLY the JSON.
