# Family F -- main family. Per-family view of the `lcar:` block in configs/default.yaml.
# default.yaml is authoritative; keep these in sync (or load via the config loader,
# which derives families from default.yaml).
name: F
K: 4
alpha: 1.0
beta: 5.0
sigma_z: 0.1
sigma_x: 0.1
eps_sigma: 0.05
cost_scale: 10000
pi: [2, 0, 3, 1]
cheap_cycle: [0, 1, 2, 3]
G_cheap: 0.1
G_exp: 1.0
